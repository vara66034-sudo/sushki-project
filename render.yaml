services:
  - type: web
    name: sushki-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GIGACHAT_API_KEY
        sync: false